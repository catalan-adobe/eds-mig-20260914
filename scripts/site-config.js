/**
 * Site-level runtime settings (dynamics DF-07). Tags stay disabled until the owner names
 * the Launch property (direction.md A-DY7); `consented.js` reads this after consent.
 */
export default {
  tags: {
    enabled: false,
    launchUrl: '',
  },
};

# us-en-adventures-html (listing archetype) — lift notes

Prototype: `stardust/prototypes/us-en-adventures-html-proposed.html` + `canon.css` (shared, untouched) + `us-en-adventures-html.css` (page) + `canon.js` (shared) + `us-en-adventures-html.js` (tabs).
Generated by `stardust/replica/lifts/build-adventures.mjs` from `us-en-adventures-html-items.json` (verbatim content extracted from the captured HTML by `adventures-items.mjs`) and `us-en-adventures-html-media-map.json`.
Served on **port 8794** (8791 is a foreign server from sd-…-0001 — never kill it).

## Gate (iter2 confirmation, --full)
| width | pixel | Δh | structural 🔴 | chrome-parity | header band | footer band | overflow |
|---|---|---|---|---|---|---|---|
| 1440 | 0.00% | 0 | 0 | ✓ | 99.99% | 100% | ok |
| 360 | 0.81% | 0 | 0 | ✓ | 100% | 100% | ok |

## Lifted values (clientlib-site.min.css, confirmed by measure.mjs --against: every box Δ0 at 1440 and 360)
- Page title: h1 Asar 40px/60px margin 27px 0 13.5px, inside `.container-fixed` WITHOUT `.col` (x 138 w 1164 at 1440; x 0 w 360 at 360).
- Hero teaser (`.cmp-teaser--hero`): full-bleed; ≥1165 image 640px cover + white content box 1136px margin-top −180 padding 0 28px; <1165 stacked, image flex:1 max-height 400 (resolves to img natural height 203 + 7px inline gap at 360), content padding 0 14px; title margin .5em 0 0; description p 18px/1.75 justify.
- Tabs (`.cmp-tabs`): tablist flex wrap, margin 18px 0 (UA `ol` default), padding-left 0; tab 14px uppercase padding .5rem 1rem, border-bottom 1px transparent, border-box, max-height/min-width 48px, line-height 21 → 38px; active bg #202020 color #fff; inactive panels display none.
- Image list: identical to landing (flex wrap, item basis 260 + 14 right padding, margin-bottom 2rem, img 200px cover, title 18/600 uppercase, description 14px dimgray uppercase nowrap ellipsis **width 260** — without the width the nowrap span blows the flex item to 1687px).
- Separator after tabs (margin 4rem 0, hr margin 9px 0 #ebebeb) — canon.
- `main.container { box-sizing: border-box; display: flow-root }` so the h1 top margin stays inside main as on live (live y 200 vs collapsed 227).
- Footer active level-1 item: `color #f7f7f7; text-decoration underline` (clientlib @132288) — missing from canon, worked around in page CSS + canon-requests.md.

## Fonts
Same as canon (Google Fonts v23 self-hosted Asar + Source Sans Pro, wknd-icon-font). No substitution.

## Interaction parity (motion-observe live → build, motion-compare)
- body.scrolly header morph ✓ (canon.js)
- tabs click → `--active` swap on tab + panel, no transition ✓ (us-en-adventures-html.js; compare reads MISSING/EXTRA only because the class is renamed cmp-tabs__tab--active → tabs__tab--active, 3× each side)
- language toggle showMenu/open ✓ (canon.js)
- Dead on live (not implemented): hover on tabs, image-list title/image links.
- Advisory: header nav hover — live probe selector not found (`.cmp-navigation__link` vs live `__item-link`); the foundation observed and implemented this hover (bg #ffea00) — canon behaviour, not invented.

## Residuals
- 360: 0.81% pixel — photo resampling inside card images (live selects 360w AEM renditions, prototype serves the 1600w capture); hot band 3000–3500 = 4.0%, text clean (crop-band3000.png).
- visual-diff STRETCHED IMAGE ×17/16: intentional `object-fit: cover` crops identical to live.
- content-diff 🟡×3 [header]: live search `<script type="x-template">` mirrored with clean class names (same as foundation).

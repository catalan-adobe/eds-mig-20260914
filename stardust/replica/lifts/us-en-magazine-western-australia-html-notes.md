# us-en-magazine-western-australia-html (article archetype) — lift notes

Prototype: `stardust/prototypes/us-en-magazine-western-australia-html-proposed.html` (generated by
`stardust/replica/lifts/build-western-australia.mjs`: chrome verbatim from the gated magazine sibling, article content
verbatim from the capture) + `canon.css` (shared, untouched) + `us-en-magazine-western-australia-html.css` + `canon.js`.
Served on **port 8794** (8791 is a foreign server from a sibling run — never reuse it).

## Gate (confirmation round, --full)
| width | pixel | Δh | structural 🔴 | chrome-parity | header crop | footer crop | overflow |
|---|---|---|---|---|---|---|---|
| 1440 | 0.00% | 0 | 0 | ✓ | 99.99% | 100% | ok |
| 360 | 0.01% | 0 | 0 | ✓ | 100% | 100% | ok |
Rounds: iter1 (--full) 0.00% / 2.94% Δ-2 (5 🔴: hidden `.cmp-contentfragment__title` h3 + up-next CTA text whitespace);
iter2 (--full) 0 🔴, 360 pixel count byte-identical to iter1 (srcset `1600w`-only was a no-op — live's stitched capture
settles on the 400w candidate); iter3 (--full, live.png re-taken) same 9840 live height → rendition confirmed; confirm round
after authoring the same srcset candidates (400w + 1600w renditions rehosted) → Δ0 / 0.01%.

## New modules lifted (clientlib-site.min.css + clientlib-base grid; measure.mjs Δ0 at 1440/360)
- Layout: inner main `default--8` (66.67%, 776px) + aside `default--3 offset--default--1` (25% + 8.33% margin-left);
  `.responsivegrid.aem-GridColumn` → padding 0 on layout containers; nested `.aem-Grid` cells are floats — `.grid`
  wrapper is `display: flow-root` + clearfix so the quote's 1em margin never collapses with the surrounding `<p>`.
- Breadcrumb (`.cmp-breadcrumb`, non-fixed): list 14px, margin 1em 0 0 (0 ≤1024), padding 12px 12px 0 0; item padding
  10px 5px uppercase; `::before` U+EA1C #ffea00; 12px ≤767.
- Pull quote (`.cmp-text--quote`): column bg #ebebeb margin 1em 0 padding 0 14px; `.cmp-text` padding 1em; blockquote
  Asar 36px margin 0 0 .5em padding 14px, `::after` 2px #ffea00 80px wide top .25em; `u` Source Sans Pro no underline.
- Content-fragment title h3: present in the DOM, `display: none` (live `.cmp-contentfragment__title`).
- Byline (`.cmp-byline`): image float left 60×60 radius 30 cover; name Asar 24px margin 0 .25rem 0 85px padding-top .5rem;
  occupations dimgray 12px uppercase margin-left 85px. Column `default--9` (75%).
- Social list (`.cmp-buildingblock--btn-list` + `.cmp-button--secondary`): column `default--3`, icon-only 48px buttons
  float left, bg #202020 / #ebebeb; padding 14px !important ≤1024 (scoped to `.contributor` — an unscoped rule shifted
  the canon footer's `.button-list` by 14px at 360).
- Up next (`.cmp-list--upnext`): float, padding-top 2rem; ul margin 1em 0 (canon resets ul → re-set), flow-root; item
  float 100% margin-bottom 1em; link float, border-left 5px #ebebeb, height 120px, padding .5rem 1rem, weight 400,
  hover bg #ffea00 / border #202020 (observed live at 360); title/date float 100% padding 5px uppercase (12px dimgray date).
- Images: live picks the 400w srcset candidate at 360 and 1600w at 1440 (no `sizes` → 100vw density); rehosted both
  renditions and authored the same candidates. Lead image carries `title="The Lennard River"` (content-diff 🟡).

## Canon workaround (already in canon-requests.md from two siblings — not re-appended)
- Footer active level-1 link `#f7f7f7` + underline: scoped rule in the page CSS.

## Interaction parity (observed → implemented)
- 1440: body.scrolly header morph (padding .5s ×2), langNav toggle (`showMenu` + `open`) — parity via canon.js
  (6 parity / 0 missing / 0 extra). Hovers probed: header nav, breadcrumb link, up-next link, social buttons, footer nav —
  none fired live at 1440.
- 360: `scrolly` parity; `.cmp-list__item-link` hover → background #ffea00 fired live and on build (hover-only run:
  `self.background` changed; in the combined run the build's `#toggleNav` click opens the panel first and masks the hover —
  the live sampler missed that click, as on the magazine sibling). `#toggleNav` → `navPanel-visible` lives in canon.js
  (observed live on the landing archetype).

## Residuals
- visual-diff STRETCHED IMAGE (byline portrait): intentional `object-fit: cover` 60×60 crop identical to live.
- content-diff 🟡×3 [header]: live search `<script type="x-template">` mirrored with clean class names — inherited from canon.
- Live rendition selection at 360 is load-order dependent (AEM lazy `src` fallback vs the 400w srcset candidate, ±2px doc
  height between plain loads); the stitched gate capture settles on the 400w state, which the build now mirrors.

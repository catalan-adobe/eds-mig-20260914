- locale-landing | `main > .section:empty { display: none }` hides a standalone `style: separator` section (the pipeline emits `<div class="separator"></div>`); exclude `.separator, .separator-space-small` from the `:empty` rule (keep `display: flow-root`) | separator-probe.mjs on the aem-cli render of /us/es: section display none → 0px; live `.cmp-separator` 1164×2 at y 904 (measure.mjs 1440) | locale-landing (stub) + every template with a trailing separator | APPLIED (C0 2026-09-24: styles/styles.css, shipped in the foundation deploy)
- locale-landing | `main .section.separator::before { margin: calc(4rem + 9px) 0 }` adds the live `.separator` 4rem and `hr` 9px margins; they COLLAPSE live (64px). Should be `margin: 4rem 0`; `separator-space-small` likewise `1em 0`, not `calc(1em + 9px)` | measure.mjs https://wknd.site/us/es.html: `.cmp-separator` marginTop 64px, main h 770 (build with calc: 715 + 9px offset, footer 988 vs live 1018) | every template using separator / separator-space-small | APPLIED (C0 2026-09-24: styles/styles.css, shipped in the foundation deploy)
- about | `scripts/scripts.js` hard-codes `document.documentElement.lang = "en"`; source pages carry `en-US` / `en-CA` (and other locales) | migrated `_meta.json` `metadata.lang` per page; `<html lang>` in stardust/migrated/*/about-us.html | all templates | QUEUED (C0 2026-09-24: scripts/ is outside the write boundary — C-final)
- program | section style `container-padded`: `main .section.container-padded { padding: 0 calc(2 * var(--spacing-md)) }` at ≤1024 — the 16 non-AEM-grid program pages stack the fixed container 14px on the column 14px below 1025 (h1 at x=28, measure.mjs harness vs prototype at 360: h1 Δx 0 only with it); shipped as a scoped override in blocks/tabs/tabs.css | program (us/ca-en adventures/* without program-grid) | APPLIED (C0 2026-09-24: styles/styles.css, shipped in the foundation deploy)
- faqs | FAQ two-column page layout (section styles `faq-aside` grid on `main:has(> .section.faq-aside)`, `faq-image` / `faq-intro` 2/3 widths ≤1024, aside `padding: 1em 0 0` for the hidden separator) lives in `blocks/accordion/accordion.css`; the accordion is the 4th section so its CSS arrives after first paint (aside stacks below, then jumps right) — move the `faq-*` section rules into `styles/styles.css` | measure.mjs harness vs prototype @1440/1024/768/360: every box Δ 0 once the block CSS is loaded; first-paint CLS visible in the loadLazy window | faqs (us/en, ca/en) | APPLIED (C0 2026-09-24: styles/styles.css, shipped in the foundation deploy)
- listing | section style `container-flush`: `main > .section.container-flush { padding: 0 }` — the listing h1 sits in the source fixed container without a grid column (no 14px gutter at any width); shipped as a scoped override in blocks/image-list/image-list.css, but a block-CSS section rule lands after the block loads (h1 shifts 14px → CLS) | measure.mjs prototype /us/en/adventures: h1 x 138 w 1164 @1440, x 0 w 360 @360; harness Δx 0 only with it | listing (us/ca-en adventures) + any template with a column-less fixed-container title | APPLIED (C0 2026-09-24: styles/styles.css, shipped in the foundation deploy)
- members-only | same `container-flush` section rule as the listing line above (`main > .section.container-flush { padding: 0 }`, currently a scoped override in blocks/image-list/image-list.css) — the members-only h1 AND its image-list section are column-less inside the source fixed container, so both sections carry `container-flush` and shift 14px when the block CSS lands late | measure.mjs harness vs prototype /ca/en/magazine/members-only @1440/360: h1, card img, card title Δx/Δy/Δw/Δh 0 with the rule | members-only (ca/en/magazine/members-only) | APPLIED (C0 2026-09-24: styles/styles.css, shipped in the foundation deploy)
- magazine-hub | section style `separator-space-medium`: `main > .section.separator-space-medium { display: flow-root }` (escape the `:empty` hide) + `main .section.separator-space-medium::before { border: 1px solid var(--color-third); content: ""; display: block; margin: 2em 0 }` — the source `.cmp-separator--space-medium` (2em margins; the hr 9px margins collapse into them); shipped as a scoped override in blocks/teaser-list/teaser-list.css, so on the published origin the rule lands when the LAST section block loads (separator pops in late → CLS) | measure.mjs harness vs prototype @1440/1024/768/360: Members Only h2, separator, teaser h2 Δy 0 once loaded | magazine-hub (us/en, ca/en) + any template with a medium separator | APPLIED (C0 2026-09-24: styles/styles.css, shipped in the foundation deploy)
- listing | CA footer link "localization features with Core Components" renders 2 px narrower than live (220→218 px) — footer-ca document / blocks/footer glyph metrics, frozen | chrome-parity.mjs --region footer @1440 + @360 on /ca/en/adventures (pub2): PAIR Δw -2px, the only chrome delta; crop-compare footer band 0.00 % both widths | every ca/en template (shared footer) — cosmetic, sub-pixel-visible

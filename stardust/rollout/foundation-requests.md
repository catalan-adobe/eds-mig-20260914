- foundation | header "Sign In" colour rgb(247,247,247) → live rgb(235,235,235) | chrome-parity.mjs 1440+360 PAIR "Sign In" color delta; crop bands pass (0.09 % / 0.53 %) | all templates
- landing | `main .section.separator` (and `.spacer`) margins collapse with the next section: its `::after` margin 4rem and the following default-content heading margin-top 27px merge into one 64px gap; live `.column` floats are BFCs so the gap is 91px | CSS analysis of styles.css L342–L356 vs canon.css L326/L332 (`.separator { margin: 4rem 0 }` + `.column { float: left }`); landing sidesteps it by authoring the "Next Adventures" h2 inside the hero-teaser section (flow-root wrapper override in blocks/hero-teaser/hero-teaser.css) — fix: `main .section:is(.separator, .spacer) { display: flow-root }` | every template with a separator followed by a heading section
- landing | `main` lets its last section margin escape: the trailing `separator` `::after` margin-bottom (4rem) collapses with `footer .footer { margin-top: 3rem }` into one 64px gap; live `.page-main` and `.site-footer` are floats, so the gap is 64 + 48 = 112px | measure.mjs 1440: live main ends 3421 → footer 3469 (48px); build main ends 3351 → footer 3415 (0px) — 48 of the 54px height delta | every template (page end); landing carries `main:has(> .section.hero-carousel-container) { display: flow-root }` in blocks/hero-carousel/hero-carousel.css until `main { display: flow-root }` lands
- landing | separator rule is 1px tall; live `hr { border-width: 1px }` (all sides, height 0) is 2px | measure.mjs 1440: live `.separator` h 2 vs build `.section.separator` h 1, two per landing page | every template with separators (2px per page on landing)
- static | `main`'s first section margin and cross-section default-content margins collapse: h1 (`margin: 27px 0 13.5px`) then the next section's h2 (`margin-top: 27px`) merge into one 27px gap; live `.page-main` and every `.column` are floats (own BFCs) so the gap is 40.5px | CSS analysis of styles.css L282–L291 (`main > .section { margin: 0 }`, `> div { padding: 0 14px }` — no vertical padding, margins pass through) vs canon.css L118 `.page-main { float: left }` + L332 `.column { float: left }`; scoped override `body.static > main, body.static main .section > .default-content-wrapper { display: flow-root }` in blocks/contributor-card/contributor-card.css — fix: `main, main .section > .default-content-wrapper { display: flow-root }` (subsumes the landing request) | every template with consecutive default-content sections (static, faqs, listing headings)
- static | header + footer do not mark the CURRENT page's nav link: live header "About Us" item carries `background-color: rgb(255, 234, 0)` (accent) and the live footer "About Us" link is `color: rgb(247, 247, 247)` + `text-decoration: underline`; build both plain — needs `aria-current="page"` (href vs location.pathname) in blocks/header/header.js + blocks/footer/footer.js and the two rules | chrome-parity.mjs 1440 pub2: header PAIR "About Us" backgroundColor rgb(255,234,0) → rgba(0,0,0,0); footer PAIR "About Us" color rgb(247,247,247) → rgb(235,235,235), textDecorationLine underline → none; 360 pub1 the same footer pair; crop bands still pass (1440 header 1.56 % / footer 0.02 %, 360 header 0.48 % / footer 0.04 %) — no block-scoped override (a nav state is chrome JS, not a page style) | every template (every page is "current" in its own nav)
- program | breadcrumb list is a block in `main .section.breadcrumb :is(ol, ul)`; live wraps it in an inline-block nav, so the list rides the wrapper's 18px/27px text baseline — 5px lower at 1440, 7px at 360 (the whole page below shifts) | measure.mjs 1440: live `main nav` y 205 vs build `ul` y 200 (main top 200 both); gate.sh pub1 360 10.82 % hot band y 0–500 21.3 %, Δh 7 — fix: `display: inline-block` on the list (program ships it scoped to body.program in blocks/mini-carousel/mini-carousel.css) | every template with a breadcrumb section (program, article)
- program | header + footer navs carry no current-section state; live marks the nav item whose href is an ancestor of the page path (`cmp-navigation__item--active`): header link on `var(--color-accent)`, footer link `color: var(--color-fg-inverse-strong); text-decoration: underline` | chrome-parity.mjs pub2 1440 PAIR "Adventures" backgroundColor rgb(255,234,0) → transparent (header), color 247→235 + underline→none (footer); crop-compare header band 1440 1.82 % (bar 2 %) — the accent block is the whole diff (header-pair crop) — fix: in header.js/footer.js add `is-active` to the li whose localePath(a.href) is a prefix of location.pathname, styled like :hover / the footer underline (program ships it scoped to body.program in blocks/mini-carousel/mini-carousel.css) | every non-home template (program, listing, article, static, unique)
- article | `main .default-content-wrapper picture img { display: block }` drops the 7px baseline descender of the source's inline `<img>` (cmp-image: `.image img { margin: 7px 0 }` inline in a block box) — a default-content lead image is 7px short and everything below shifts | measure.mjs 1440 prototype `.lead-image .image` h 723 vs img 702 + 14 margins = 716 (7px descender); block CSS override `body.article main .default-content-wrapper picture img { display: inline }` in blocks/article-body/article-body.css — fix: keep the img inline (block `picture`, inline `img`) for default-content images site-wide | article (lead image); any template with a default-content image
- listing | section style `page-title`: `main > .section.page-title > div { padding: 0 }` — live listing pages put `h1.page-title` directly in `.container--fixed` (x 138, w 1164 at 1440), not in a 14px `.column` gutter | measure.mjs 1440 prototype h1 x 138 w 1164 vs harness x 152 w 1136 before the scoped override (tabs.css `main:has(.tabs.cards) > .section.page-title > div`); live magazine keeps the gutter (h1 x 152 / x 14), so only adventures carries the style | listing (adventures)
- listing | separator `space-medium` variant: live `.cmp-separator--space-medium` margins are 2em (foundation `separator` = 4rem) and the hr is 2px (border 1px all sides); the preceding paragraph's 13.5px margin stacks on it (live floats) but collapses under `display: block !important` — a `separator space-medium` section style (`::after { margin: 2em auto; border-top-width: 2px }`) + a collapse-proof section box (padding-top 0.01px or flow-root without the !important) | measure.mjs 1440 migrated magazine hr y 1764 vs harness 1751 before the scoped override (list-teaser.css `main .section.separator:has(+ .section.list-teaser-container)::after { margin: calc(2em + 13.5px) auto 2em }`) | listing (magazine); any page using the space-medium separator
